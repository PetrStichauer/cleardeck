// Storage keys kept for reference only (data is now stored in Supabase tables)
export const STORAGE_KEYS = {
  tasks: 'tasks',
  projects: 'projects',
  tags: 'cd_tags',
  folders: 'cd_folders',
  perspectives: 'cd_perspectives',
} as const;
